const model = require('../models');

// calls model query funcs and handles response
module.exports = {
  getReviews: null,
  getReviewMeta: null,
  postReview: null,
  markReviewHelpful: null,
  reportReview: null
};